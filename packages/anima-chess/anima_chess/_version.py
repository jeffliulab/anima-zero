"""Version of the rules library.

Deliberately independent of ANIMA's own version: this package is a self-contained library
that happens to live in the same repository, and tying the two numbers together would mean
bumping one for changes that never touched it.

规则库自己的版本号。

**有意**与 ANIMA 的版本号脱钩：这个包是一个碰巧住在同一个仓库里的自足库，把两个号绑在一起，
只会导致其中一个为了跟自己无关的改动而升版。
"""

__version__ = "0.1.0"

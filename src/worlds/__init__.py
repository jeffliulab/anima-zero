"""Worlds that ship with the brain.

Normally a world is a separate process in its own repository — that separation is what AWI
is for. What lives here is the exception: the one small world the wheel carries so that
`pip install anima` leads somewhere. See `desk/world.py`.

随大脑一起分发的世界。

正常情况下一个世界是独立仓库里的独立进程——那种分离正是 AWI 的意义。住在这里的是例外：
wheel 带着的那一个小世界，好让 `pip install anima` 有个去处。见 `desk/world.py`。
"""
